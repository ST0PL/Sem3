<Window x:Class="ILS_WPF.Views.Warehouses.AddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:clib="clr-namespace:ControlsLib.Controls;assembly=ControlsLib"
        xmlns:local="clr-namespace:ILS_WPF.Views.Warehouses"
        mc:Ignorable="d"
        Title="Регистрация данных склада" Height="250" Width="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Border CornerRadius="20"
            BorderThickness="1"
            BorderBrush="#333e50"
            Background="#101828">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border Background="#80141d2b"
                    BorderThickness="0,0,0,1"
                    CornerRadius="20,20,0,0"
                    BorderBrush="#806b6b6b"
                    Padding="10"
                    MouseDown="Border_MouseDown">
                <clib:Button Style="{StaticResource CloseButtonStyle}"
                             Click="CloseButton_Click"
                             Opacity="0.8"
                             HorizontalAlignment="Right"/>
            </Border>
            <Grid Grid.Row="1"
                  Margin="30,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Регистрация данных склада"
                           Foreground="#00D492"
                           FontWeight="SemiBold"
                           FontSize="20"/>
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            Margin="0,20,0,0">
                    <clib:TextBox Style="{StaticResource TextBoxStyle}"
                                  Placeholder="Название склада"
                                  Width="360"
                                  Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                    <clib:ComboBox Style="{StaticResource ComboBoxStyle}"
                                   Width="175"
                                   Margin="10,0,0,0"
                                   ItemsSource="{Binding WarehouseTypes}"
                                   SelectedItem="{Binding CurrentType}">
                        <clib:ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource enumConverter}}"
                                           Margin="5"
                                           Foreground="White"/>
                            </DataTemplate>
                        </clib:ComboBox.ItemTemplate>
                    </clib:ComboBox>
                </StackPanel>
                <clib:Button Grid.Row="2" 
                             Style="{StaticResource DarkGreenButtonStyle}"
                             Content="Зарегистрировать"
                             HorizontalAlignment="Right"
                             Margin="10,25,25,10"
                             Padding="20,10,20,10"
                             Command="{Binding RegisterCommand}"
                             Click="CloseButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
